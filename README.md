# ERASERHEAD

In order to run a clone of this bot you will need:
* a .env file with contents: `TOKEN=YOUR_BOT_TOKEN_HERE`
* to run `npm install` to install all package dependencies

A .env file will not be required if you can supply the node environment variables externally

In order to spin up the bot, run `npm start`
Enjoy!